test_subpathwayToGraph <- function() 
{
	message('Testing subpathwayToGraph', appendLF=FALSE)
    
	load(system.file('extdata', 'data.RData', package='DEsubs'))

   all.equal(1, 1)

   message('done.')
}
